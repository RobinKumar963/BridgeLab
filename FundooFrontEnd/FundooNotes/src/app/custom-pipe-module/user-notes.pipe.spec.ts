import { UserNotesPipe } from './user-notes.pipe';

describe('UserNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new UserNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
